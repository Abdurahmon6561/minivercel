# Supabase project URL and the ANON key. The anon key is public by design and is
# the only Supabase key that may ever appear in the browser.
# The service_role key must NEVER be used here (SPEC.md non-negotiable #4).
VITE_SUPABASE_URL=https://<ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon key>

# The API. web/.env.production sets this to https://api.getdropbin.xyz for
# the production build (Dockerfile's webbuild stage); override here only for
# local dev against a different API.
VITE_API_BASE_URL=https://api.getdropbin.xyz

# GitHub OAuth scope requested at sign-in. `public_repo` is least privilege and
# enough for Phase 3 on public repositories; use `repo` only if you need private
# ones, and understand that it is read/write on every repo the user can see.
# `public_repo` is NOT sufficient: it does not cover webhooks, and committing
# the Phase 4 workflow file needs `workflow` (which `repo` does not imply).
# Public repos only, minimum: public_repo,admin:repo_hook,workflow
VITE_GITHUB_SCOPES=repo,workflow
